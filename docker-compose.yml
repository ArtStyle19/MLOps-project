version: "3.9"
services:
  backend:
    build: ./backend
    container_name: flask_backend
    ports:
      - "5000:5000"

  frontend:
    build: ./frontend
    container_name: react_frontend
    ports:
      - "80:80"
    depends_on:
      - backend
